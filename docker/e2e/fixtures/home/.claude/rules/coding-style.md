# Coding Style

- Prefer `const` over `let`. Mutate only when required.
- Functions over classes for stateless logic.
- No default exports in TypeScript modules.
- Filenames are kebab-case; types are PascalCase.
- Keep functions under 40 lines unless there is a clear reason.
