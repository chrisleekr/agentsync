# Codex Agents

Top-level instructions for Codex CLI on this machine.

- Prefer concise diffs over rewrites.
- Always read `config.toml` before proposing tool calls.
- When in doubt about a profile, default to `profiles.default`.

Project-specific overrides live in each repo's own `AGENTS.md`.
