# Codex Agents — Override

This file overrides `AGENTS.md` for short-lived experiments.

When present, Codex prefers the rules here over the base document.
Remove this file to fall back to the canonical `AGENTS.md`.

Currently active overrides:

- Allow long-running shells without re-prompting.
