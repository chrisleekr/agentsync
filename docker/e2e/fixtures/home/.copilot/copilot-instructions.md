# Copilot Instructions

Global preferences for GitHub Copilot CLI on this machine.

- Default language is TypeScript unless the repo says otherwise.
- Suggest tests alongside any non-trivial code change.
- Never propose pushing to `main` directly.
- Cite the file and line for any reference to existing code.
