# /refactor

Refactors the selected range without changing observable behaviour.

Constraints:

- Tests must still pass without modification.
- No new dependencies.
- Preserve the public API exactly.

Reports a short diff summary at the end.
