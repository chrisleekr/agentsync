# Pinned for reproducible local + CI docs builds.
# Bump intentionally — Material occasionally renames features between minors.
mkdocs==1.6.1
mkdocs-material==9.7.6
pymdown-extensions==11.0.2

# Privacy plugin (CI-only, see mkdocs.yml) needs no extra deps.
# Social-card plugin removed — would require Pillow + cairosvg + libcairo2
# system lib, and offered only OG/Twitter preview images.
